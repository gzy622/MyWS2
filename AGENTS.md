# 智能体工作规则

本文件是本仓库智能体工作的唯一入口。规则只约束工作方式，不替代产品、交互、视觉和工程事实。

## 工作基线

1. 用户当前任务是最高优先级；与现行文档冲突时，说明冲突并按用户明确要求执行。
2. 开始前说明本次纳入与不纳入的范围，以及受影响的页面、状态和文件；无法确定时先做只读检查，不臆测业务决定。
3. 只读取与改动职责直接相关的文档或章节；不以通读全部文档代替定位。
4. 行为、固定文案、文件职责或公开契约变化时，同步更新对应现行文档。
5. 不执行未获授权的外部操作、数据删除、依赖升级、浏览器自动验收或 Android 链路测试。浏览器自动验收包括 `tools/verify-web.ps1`、Edge/CDP 和为浏览器调试启动的服务；Android 链路测试包括同步、构建、部署、真机检查和 adb 调试。
6. 每次改动后只运行与改动直接相关的快速检查，并如实报告已执行、未执行和失败项。
7. 不覆盖或回退用户已有改动。发现与任务重叠的未提交内容时，先保留并说明影响。

## 额度控制

| 适用范围 | 触发条件 | 执行与核对方式 | 不适用范围 |
| --- | --- | --- | --- |
| 故障定位、源码阅读、差异检查和测试 | 用户描述存在多种解释，或文件、差异、日志、测试输出预计较长 | 修改前先确认可观察现象；优先使用 `rg` 精确定位，只读取命中位置附近内容；工具输出通常限制在 8,000～12,000 tokens；避免重复读取同一内容；相关测试先小范围执行，完整测试通常只运行一次；交付前核对工具输出和测试次数 | 用户明确要求完整内容；定位失败需要扩大读取范围；失败详情、安全检查或数据完整性检查需要更多输出 |

### RTK 试用

- 开始任务时可用 `Get-Command rtk -ErrorAction SilentlyContinue` 检查 RTK；未安装时继续使用原命令，不中断任务。
- RTK 只用于预计输出较长的只读命令和检查命令，例如 `rtk git status`、`rtk git diff`、`rtk git log`、`rtk grep` 和 `rtk test <测试命令>`。
- 精确读取源码、检查完整差异、调查失败、安全检查和执行复杂 PowerShell 命令时使用原命令。
- `git add`、`git commit`、`git push` 等会改变本地或远端状态的命令继续直接使用 Git。
- RTK 输出信息不足时直接改用原命令，不重复尝试不同压缩等级；不得将 RTK 的压缩结果视为完整输出。
- 不运行 `rtk init -g --codex`，避免修改 Codex 全局说明；试用安装、配置和评价方式见 `docs/guides/development.md`。

## 文档阅读路由

| 任务类型 | 必读内容 |
| --- | --- |
| 任意代码改动 | `docs/engineering.md` 的「最小必读」，以及 §3 中对应的映射行 |
| CSS、图标或视觉状态 | 另读 `docs/visual-design.md` |
| 状态、手势、返回栈或浮层行为 | 另读 `docs/interaction.md` 对应章节 |
| 页面、功能范围或固定文案 | 另读 `docs/product.md` |
| 模块职责或跨模块依赖 | 另读 `docs/architecture.md` |
| 命名或文案用语 | 另读 `docs/glossary.md` 对应章节 |
| 启动、测试、导出或 Android 操作 | `docs/guides/development.md` |
| 重建工作规则、维护文档或处理冲突 | `docs/agent-workflow.md`；需要文档地图时再读 `docs/README.md` |

## 子智能体简称

以下约定只在用户明确使用对应简称要求调用子智能体时生效；未指定简称时，不据此推定模型或思考等级。

| 用户用语 | 模型 | 思考等级 |
| --- | --- | --- |
| Luna 子智能体 | GPT-5.6 Luna | `Max` |
| Terra 子智能体 | GPT-5.6 Terra | `High` |

- 子智能体称呼可以直接书写，也可以使用 `「」`，例如 Luna 子智能体或「Luna 子智能体」；符号不影响含义。
- 调用前核对子智能体的模型与思考等级是否符合上表，不能只把 Luna 或 Terra 写成任务名称。
- 指定模型或思考等级在当前环境不可用时，先向用户说明，不擅自换用其他模型或继承当前智能体配置。
- 本约定不要求在用户未指定子智能体时主动派出子智能体，也不改变具体任务的授权范围。

## Pi 子智能体调用

- 适用范围：用户要求调用子智能体，或当前任务明确允许委派工作时，可以使用本机安装的 Pi Coding Agent 执行工作区内任务。
- 调用入口：使用 `node tools/pi-agent-webui/control.mjs` 分派、查看和停止任务；需要启动本机状态服务时运行 `npm run pi:webui`。WebUI 只是可选的状态查看页面，不影响 Pi 作为子智能体使用。
- 模型选择：Luna 子智能体使用 `luna`，DeepSeek V4 Flash 使用 `flash`；用户指定其他模型时，先确认本机 Pi 是否可用该模型。
- 工作权限：Pi 子智能体可以在当前工作区内读取和写入文件，并可使用 Shell 运行任务需要的本地命令，仍须遵守用户授权、文档阅读路由、受保护路径和验证要求。Shell 命令不得访问或修改工作区外文件。
- 核对方式：调用后检查返回的模型、思考等级和任务状态；完成后读取结果，并如实说明 Pi 执行的修改与检查。
- 不适用范围：用户指定其他子智能体方式、当前环境无法运行 Pi，或任务没有获得委派授权时，不使用该入口。
- 详细命令和安全限制见 `docs/guides/development.md` 的「Pi 子智能体 WebUI」。

## 三方协作称呼

- 用户：项目拥有者、出资者、任务负责人、测试者、当前使用者和最终确认者；也可写作 User，兼容称呼为 Owner。
- Lead：高级工程职责，负责复杂分析、计划制定和技术检查。
- Maker：实施职责，负责常规修改和按计划执行；兼容称呼为 Dev。
- 英文称呼不区分大小写。称呼可以直接书写，也可以使用 `「」` 或 `【】`，例如 Lead、「lead」与【Lead】含义相同。
- 只在用户当前任务中将这些词用作称呼或分工时识别为协作角色；代码、路径、日志、引用内容和产品文档中的同名词按原有含义理解。
- Lead 和 Maker（含兼容称呼 Dev）不固定对应某个 Agent 软件或模型，以用户在当前任务中的分配为准；用户未指定时，不自行推定。
- 角色称呼完全可选。用户直接发出任务指令时，接收指令的 Agent 按正常方式理解并执行；不得要求用户先安排角色，也不得因缺少角色称呼增加确认步骤。
- `【】` 保留中文内容中的常见用途；项目文档中的 `「」` 通常标记界面实际显示的文字，用户任务中的角色称呼依照上条识别；其他符号按中文文档和代码的常见方式使用。

## 交付约定

- 改动按职责分批提交。仅当本次产生可提交改动时，最终回复提供一条 Conventional Commits 格式的提交信息：类型使用英文，正文使用简体中文。
- `docs/agent-workflow.md` 定义工作规则的边界、冲突处理和重建方式；`docs/README.md` 只维护文档地图。
