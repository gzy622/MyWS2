# 智能体工作规则

本文件是本仓库智能体工作的唯一入口，只约束工作方式；规则边界与维护方式见 `docs/agent-workflow.md`。

## 工作基线

1. 用户当前任务优先；与现行文档冲突时，说明冲突并按用户明确要求执行。
2. 开始前说明纳入与不纳入范围及受影响页面、状态和文件；无法确定时先只读定位，不臆测业务决定。
3. 只读职责直接相关的文档或章节；行为、固定文案、文件职责或公开契约变化时同步对应现行文档。
4. 不执行未获授权的外部操作、数据删除、依赖升级、浏览器自动验收或 Android 链路测试。浏览器验收包括 `tools/verify-web.ps1`、Edge/CDP 和为调试启动的服务；Android 链路包括同步、构建、部署、真机检查和 adb 调试。
5. 改动后只运行直接相关的快速检查，如实报告已执行、未执行和失败项。
6. 不覆盖或回退用户已有改动；发现任务重叠的未提交内容时先保留并说明影响。

## 额度与工具

- 用户描述有多种解释，或差异、日志、测试输出预计较长时，修改前先确认可观察现象；优先 `rg` 精确定位，工具输出通常控制在 8,000～12,000 tokens，避免重复读取，相关测试先小范围执行，完整测试通常只运行一次。用户明确要求完整内容，或定位失败、失败调查、安全和数据完整性检查需要时，可扩大范围。
- RTK 仅可用于预计输出较长的只读命令和检查；精确源码、完整差异、失败调查、安全检查、复杂 PowerShell 及写操作使用原命令。RTK 不可用时直接继续；输出不足时改用原命令，不运行 `rtk init -g --codex`。安装与评价方式见 `docs/guides/development.md`。

## 文档阅读路由

| 任务类型 | 必读内容 |
| --- | --- |
| 任意代码改动 | `docs/engineering.md`「最小必读」及 §3 对应映射 |
| CSS、图标或视觉状态 | `docs/visual-design.md` |
| 状态、手势、返回栈或浮层 | `docs/interaction.md` 对应章节 |
| 页面、功能范围或固定文案 | `docs/product.md` |
| 模块职责或跨模块依赖 | `docs/architecture.md` |
| 命名或文案用语 | `docs/glossary.md` 对应章节 |
| 启动、测试、导出或 Android | `docs/guides/development.md` |
| 工作规则、文档维护或冲突 | `docs/agent-workflow.md`；需要文档地图时读 `docs/README.md` |

## 子智能体

只有用户要求或当前任务明确授权委派时才调用。用户明确使用下列简称时，必须匹配对应模型与思考等级；不可用时先说明，不擅自替换。

| 用户用语 | 扩展 Agent | 模型 / 思考等级 |
| --- | --- | --- |
| Luna 子智能体 | 皓月 | GPT-5.6 Luna / `Max` |
| Terra 子智能体 | 远山 | GPT-5.6 Terra / `High` |
| DeepSeek，或未指定模型的常规任务 | 虎鲸 | DeepSeek V4 Flash / `High` |

- 复杂分析、关键实施和技术复核使用皓月；用户指定其他模型时先确认可用性。
- 有 `subagent` 工具时使用 Pi 扩展；不可用时才使用 `node tools/pi-agent-webui/control.mjs`，需要状态服务时运行 `npm run pi:webui`。同一任务不得同时走两条路由；WebUI 的 Luna 为 `luna`、DeepSeek 为 `flash`，不支持 Terra 时必须说明。
- 子智能体只能处理当前工作区，仍受用户授权、文档路由、受保护路径和验证边界约束；Shell 不得访问或修改工作区外文件。
- 调用后核对实际模型、思考等级、名称和状态并读取结果。DeepSeek 系列名称必须为鲸类名称，GPT-5.6 Luna 名称必须以「月」结尾；其他模型不适用此命名规则。
- 详细配置、命令与安全限制见 `docs/guides/development.md`「Pi 子智能体调用」。

## 协作称呼

- 用户也可称 User 或 Owner；Lead 负责复杂分析、计划和技术检查；Maker（兼容 Dev）负责常规实施。
- 称呼不区分英文大小写，可省略或使用 `「」`、`【】`；仅在当前任务明确用于分工时识别，代码、路径、日志和引用中的同名词按原义理解。
- Lead、Maker 不绑定具体 Agent 或模型，称呼完全可选；不得因用户未分配角色而增加确认步骤。

## 交付约定

- 改动按职责分批提交。仅当本次产生可提交改动时，最终回复提供一条 Conventional Commits 提交信息：类型用英文，正文用简体中文。
- `docs/README.md` 只维护文档地图；长期规则的新增与冲突处理遵循 `docs/agent-workflow.md`。
